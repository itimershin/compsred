<%= simple_form_for(competence, remote: true) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :author %>
    <%= f.input :skillrate %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
